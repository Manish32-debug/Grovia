# Empty means "same origin", which routes through the Vite dev proxy in
# development and through the platform rewrite in production. Set an absolute
# URL only when the API lives on a different host.
VITE_API_BASE_URL=
